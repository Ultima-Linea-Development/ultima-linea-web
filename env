PORT=3000

# MongoDB - Para MongoDB local (Docker Compose)
MONGODB_URI=mongodb://root:rootpassword@localhost:27017
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_USER=root
MONGODB_PASSWORD=rootpassword
MONGODB_DATABASE=camisetas

# JWT
JWT_SECRET=tu-clave-secreta-cambiala-en-produccion

# CORS (referencia del backend original; Next.js sirve front y API en el mismo origen)
CORS_ORIGIN=http://localhost:3000

# Storage (desarrollo local: carpeta relativa; las imágenes se sirven en /storage)
STORAGE_DIR=storage
STORAGE_URL=http://localhost:3000/storage

# Frontend (opcional: apuntar a API externa Go durante transición)
# NEXT_PUBLIC_API_URL=http://localhost:8080
